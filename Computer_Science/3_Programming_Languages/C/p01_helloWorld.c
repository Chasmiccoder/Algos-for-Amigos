// Comment lines
#include <stdio.h>

int main() {
    printf("Hello World!\n");
    int $a$ = 2; // can declare with vars with some special characters as well. (Not recommended)
    printf("%d", $a$);
    return 0;
}